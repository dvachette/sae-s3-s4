version : "3.9"

services:
  backend:
    build: ./server
    env_file: 
      - ./server/.env
    ports:
      - "3000:3000"
    restart: unless-stopped
  
  frontend:
    build: ./TCG_web
    ports:
      - "8080:80"
    depends_on:
      - backend
    restart: unless-stopped